# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

Приложение работает с двумя основными сущностями данных — **товар** и **покупатель**.  
Эти сущности описываются интерфейсами и используются в моделях данных.

### Интерфейсы данных

#### Товар (IProduct)

`interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}`

Назначение:  
Описывает товар, доступный для покупки в приложении.  
Поле `price` может иметь значение `null`, если цена товара отсутствует или товар недоступен.

#### Покупатель (IBuyer)

`type TPayment = string;
interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}`

Назначение:  
Описывает данные покупателя, которые необходимо указать при оформлении заказа.

## Модели данных

Модели данных отвечают за хранение и обработку состояния приложения.  
Они не работают с DOM и не содержат логики отображения пользовательского интерфейса.

### Каталог товаров (ProductsCatalog)

Назначение и зона ответственности:  
- хранение полного списка товаров, полученных с сервера;
- хранение выбранного товара для подробного отображения.

Конструктор:  
`constructor()`

Поля класса:  
`products: IProduct[]` — массив всех товаров каталога;  
`preview: IProduct | null` — товар, выбранный для подробного отображения.

Методы класса:  
`setProducts(items: IProduct[]): void` — сохраняет массив товаров, полученный извне;  
`getProducts(): IProduct[]` — возвращает массив товаров из модели;  
`getProductById(id: string): IProduct | undefined` — возвращает товар по его `id`;  
`setPreview(item: IProduct): void` — сохраняет товар для подробного отображения;  
`getPreview(): IProduct | null` — возвращает выбранный товар.

### Корзина (Basket)

Назначение и зона ответственности:  
- хранение товаров, выбранных пользователем для покупки;
- управление состоянием корзины и выполнение вычислений.

Конструктор:  
`constructor()`

Поля класса:  
`items: IProduct[]` — массив товаров, находящихся в корзине.

Методы класса:  
`getItems(): IProduct[]` — возвращает массив товаров в корзине;  
`add(item: IProduct): void` — добавляет товар в корзину;  
`remove(item: IProduct): void` — удаляет товар из корзины;  
`clear(): void` — очищает корзину;  
`getTotal(): number` — возвращает общую стоимость товаров в корзине;  
`getCount(): number` — возвращает количество товаров в корзине;  
`has(id: string): boolean` — проверяет наличие товара в корзине по его `id`.

### Покупатель (Buyer)

Назначение и зона ответственности:  
- хранение данных покупателя;
- частичное обновление данных без потери уже сохранённых значений;
- валидация данных перед отправкой заказа.

Конструктор:  
`constructor()`

Поля класса:  
`payment: TPayment | ''` — выбранный способ оплаты;  
`email: string` — электронная почта покупателя;  
`phone: string` — телефон покупателя;  
`address: string` — адрес доставки.

Методы класса:  
`setData(data: Partial<IBuyer>): void` — сохраняет данные покупателя частично;  
`getData(): IBuyer` — возвращает все данные покупателя;  
`clear(): void` — очищает данные покупателя;  
`validate(): Partial<Record<keyof IBuyer, string>>` — выполняет валидацию данных.

Пример результата валидации:  
`{
  payment: 'Не выбран вид оплаты',
  email: 'Укажите емэйл'
}`

## Слой коммуникации

Для взаимодействия с сервером используется отдельный класс коммуникационного слоя.  
Он не хранит состояние приложения и не работает с DOM. Его задача — выполнять запросы к API и возвращать данные в удобном виде.

Класс использует **композицию**: в конструктор передается объект, реализующий интерфейс `IApi`, а внутри методов вызываются `get` и `post`.

### Класс WebLarekApi

Назначение и зона ответственности:  
- получение каталога товаров с сервера;
- отправка заказа на сервер.

Конструктор:  
`constructor(api: IApi)` — принимает объект, реализующий интерфейс `IApi`.

Поля класса:  
`api: IApi` — экземпляр API-клиента, через который выполняются запросы.

Методы класса:  
`getProducts(): Promise<IProduct[]>` — выполняет GET запрос на эндпоинт `/product/` и возвращает массив товаров;  
`createOrder(order: IOrder): Promise<IOrderResult>` — выполняет POST запрос на эндпоинт `/order/` и отправляет данные заказа, полученные в параметрах метода.

---
# Документация классов слоя Представления (View)

Этот раздел описывает все классы View-слоя, необходимые для отображения сайта **Web-Larek** по макету и текущей вёрстке.  
View-слой отвечает **только за DOM и пользовательские действия**: получает данные от Presenter, отображает их и **генерирует события** на каждое действие пользователя.

## Ключевые правила

- **Один класс View = один блок разметки** (один `container`).
- Три карточки (разные templates) имеют **общего родителя** `Card`.
- Две формы имеют **общего родителя** `Form<T>`.
- `Modal` **не имеет наследников**. Контент модального окна — самостоятельные компоненты (карточка, корзина, формы, экран успеха).
- Любое действие пользователя → `emit(...)`, событие обрабатывает Presenter.


## Компоненты страницы

### Header

Компонент шапки сайта.

#### Публичный API

- `set counter(value: number)` — обновить значение счётчика.

#### События

- `basket:open` — клик по `.header__basket`.

### Gallery (`.gallery`)

Контейнер каталога (список карточек на главной странице).

#### DOM

- `.gallery` — контейнер карточек.

#### Данные

```ts
type GalleryData = {
  catalog: HTMLElement[];
};
```

#### Публичный API

- `set catalog(items: HTMLElement[])` — заменить содержимое галереи.

#### События

- События напрямую не генерирует (генерируются карточками товаров).


## Модальное окно

### Modal (`#modal-container`)

Оболочка модального окна. **Не имеет дочерних классов**.  
Любой компонент (корзина, форма, карточка, экран успеха) может быть отрисован внутри модального окна.

#### DOM

- `#modal-container` — корневой элемент
- `.modal__close` — закрытие
- `.modal__content` — контейнер контента

#### Данные

```ts
type ModalData = {
  content: HTMLElement;
};
```

#### Публичный API

- `open(): void` — открыть модальное окно
- `close(): void` — закрыть модальное окно
- `set content(node: HTMLElement)` — вставить контент в `.modal__content`

#### События

- `modal:close` — закрытие пользователем (кнопка / оверлей / ESC — если реализовано)

## Карточки товаров

Карточки используют три HTML-шаблона:

- `#card-catalog`
- `#card-preview`
- `#card-basket`

Все карточки наследуются от общего родителя **Card**, в который вынесен общий функционал:
заголовок, цена, категория, изображение и логика рендера.

### Card

Базовый класс карточки товара.

#### Данные

```ts
type CardData = {
  id: string;
  title: string;
  price: number | null;
  category?: string;
  image?: string;
  description?: string;
  index?: number;
};
```

#### Публичный API

- `set title(value: string)`
- `set price(value: number | null)`
- `set category(value: string)`
- `set image(value: { src: string; alt?: string })`

### CatalogCard (`#card-catalog`)

Карточка товара в каталоге.

#### DOM

- `button.gallery__item.card` — корневой элемент
- `.card__category`
- `.card__title`
- `.card__image`
- `.card__price`

#### События

- `card:select` — клик по карточке (открыть превью).
  Payload: `{ id }`

### PreviewCard (`#card-preview`)

Подробная карточка товара (обычно отображается в модальном окне).

#### DOM

- `.card__image`
- `.card__category`
- `.card__title`
- `.card__text`
- `.card__price`
- `.card__button` — кнопка «В корзину»

#### Публичный API

- `set description(value: string)` — заполнить `.card__text`

#### События

- `product:buy` — клик по кнопке «В корзину».
  Payload: `{ id }`

### BasketItemCard (`#card-basket`)

Строка товара в корзине.

#### DOM

- `.basket__item-index`
- `.card__title`
- `.card__price`
- `.basket__item-delete` — кнопка удаления

#### Публичный API

- `set index(value: number)` — установить номер позиции в корзине

#### События

- `basket:remove` — удаление товара.
  Payload: `{ id }`

## Корзина

### Basket (`#basket`)

Компонент корзины: список товаров, сумма и кнопка оформления заказа.

#### DOM

- `.basket__list` — список позиций
- `.basket__price` — сумма заказа
- `.basket__button` — кнопка «Оформить»

#### Данные

```ts
type BasketData = {
  items: HTMLElement[];
  total: number;
};
```

#### Публичный API

- `set items(value: HTMLElement[])` — отрисовать список `.basket__list`
- `set total(value: number)` — обновить сумму

#### События

- `order:open` — клик по кнопке оформления

---

# События приложения (EventEmitter)

В этом разделе описаны **все события**, которые генерируются слоем Представления (View).  
Каждое действие пользователя **обязательно приводит к генерации события**, которое обрабатывается Презентером.


### События Header
- **`basket:open`** — пользователь нажал на кнопку корзины в шапке (`.header__basket`).  
  Payload: отсутствует.


### События Modal
- **`modal:open`** — модальное окно открыто методом `modal.open()`.  
  Payload: отсутствует.
- **`modal:close`** — модальное окно закрыто пользователем (кнопка закрытия или клик по оверлею).  
  Payload: отсутствует.

### События карточек товара

#### CatalogCard (`#card-catalog`)
- **`card:select`** — клик по карточке товара в каталоге (открыть превью).  
  Payload: `{ id: string }`.

#### PreviewCard (`#card-preview`)
- **`product:buy`** — клик по кнопке «В корзину» в карточке предпросмотра товара.  
  Payload: `{ id: string }`.

#### BasketItemCard (`#card-basket`)
- **`basket:remove`** — клик по кнопке удаления товара из корзины.  
  Payload: `{ id: string }`.

### События Basket (`#basket`)
- **`order:open`** — клик по кнопке «Оформить» в корзине.  
  Payload: отсутствует.

### События формы заказа OrderForm (`#order`)
- **`order:payment`** — выбор способа оплаты (кнопка «Онлайн» или «При получении»).  
  Payload: `{ payment: 'card' | 'cash' }`.
- **`order:change`** — изменение адреса доставки.  
  Payload: `{ field: 'address', value: string }`.
- **`order:submit`** — отправка формы заказа (кнопка «Далее»).  
  Payload: отсутствует.

### События формы контактов ContactsForm (`#contacts`)
- **`contacts:change`** — изменение значения поля контактов.  
  Payload: `{ field: 'email' | 'phone', value: string }`.
- **`contacts:submit`** — отправка формы контактов (кнопка «Оплатить»).  
  Payload: отсутствует.

### События экрана успеха Success (`#success`)
- **`success:close`** — клик по кнопке «За новыми покупками!».  
  Payload: отсутствует.

  ---

# События моделей данных

В этом разделе описаны события, которые генерируются **моделями данных** при любом изменении их состояния.  
Эти события обрабатываются Презентером, который обновляет View.

### ProductsCatalog (Model)
- **`catalog:changed`** — обновлён список товаров в каталоге (`setProducts`).  
  Payload: `{ items: IProduct[] }`

- **`preview:changed`** — выбран товар для подробного просмотра (`setPreview`).  
  Payload: `{ item: IProduct | null }`

### Basket (Model)
- **`basket:changed`** — изменилось состояние корзины (добавление / удаление / очистка).  
  Payload: `{ items: IProduct[]; total: number; count: number }`

### Buyer (Model)
- **`buyer:changed`** — изменились данные покупателя (`setData`).  
  Payload: `Partial<IBuyer>`

---

# Презентер

Презентер — это центральный слой приложения, который содержит всю бизнес-логику страницы.  
Он связывает **Модели данных** и **Представления**, обрабатывая события и управляя изменением состояния приложения.

В приложении используется **один презентер**, реализованный в файле `main.ts`, так как приложение является одностраничным.

## Ответственность презентера

Презентер отвечает за:

- подписку на события от **View** и **Model**;
- обработку пользовательских действий;
- вызов методов моделей для изменения данных;
- управление отображением компонентов;
- открытие и закрытие модальных окон;
- формирование и отправку заказа на сервер.

## События от моделей данных

### `catalog:changed`
Источник: `ProductsCatalog`  
Описание: каталог товаров был загружен или обновлён.

Действия презентера:
- получает массив товаров из модели;
- создаёт карточки `CatalogCard`;
- передаёт их в `Gallery` для отображения.

### `preview:changed`
Источник: `ProductsCatalog`  
Описание: выбран товар для предпросмотра.

Действия презентера:
- создаёт `PreviewCard`;
- открывает модальное окно с карточкой товара.

### `basket:changed`
Источник: `Basket`  
Описание: содержимое корзины изменилось.

Действия презентера:
- обновляет счётчик корзины в `Header`;
- перерисовывает список товаров в `Basket`;
- обновляет итоговую сумму;
- управляет доступностью кнопки оформления заказа.

### `buyer:changed`
Источник: `Buyer`  
Описание: изменились данные покупателя.

Действия презентера:
- выполняет валидацию данных;
- обновляет состояние `OrderForm`;
- обновляет состояние `ContactsForm`;
- управляет доступностью кнопок отправки форм.

## События от представлений

### Работа с каталогом

#### `card:select`
Источник: `CatalogCard`  
Описание: пользователь выбрал карточку товара.

Действия презентера:
- получает товар по `id`;
- устанавливает его как `preview` в модели каталога.

#### `product:buy`
Источник: `PreviewCard`  
Описание: пользователь добавил товар в корзину.

Действия презентера:
- добавляет товар в модель `Basket`.

### Работа с корзиной

#### `basket:open`
Источник: `Header`  
Описание: пользователь нажал на кнопку корзины.

Действия презентера:
- открывает модальное окно с компонентом `Basket`.

#### `basket:remove`
Источник: `BasketItemCard`  
Описание: пользователь удалил товар из корзины.

Действия презентера:
- удаляет товар из модели `Basket`.

#### `order:open`
Источник: `Basket`  
Описание: пользователь начал оформление заказа.

Действия презентера:
- открывает форму `OrderForm` в модальном окне.

## Работа с формами

### `order:payment`
Источник: `OrderForm`  
Описание: пользователь выбрал способ оплаты.

Действия презентера:
- сохраняет способ оплаты в модели `Buyer`.

### `order:change`
Источник: `OrderForm`  
Описание: изменён адрес доставки.

Действия презентера:
- сохраняет адрес в модели `Buyer`.

### `order:submit`
Источник: `OrderForm`  
Описание: пользователь переходит ко второй форме оформления.

Действия презентера:
- открывает форму `ContactsForm`.

### `contacts:change`
Источник: `ContactsForm`  
Описание: пользователь изменил email или телефон.

Действия презентера:
- сохраняет данные в модели `Buyer`.

### `contacts:submit`
Источник: `ContactsForm`  
Описание: пользователь подтверждает оформление заказа.

Действия презентера:
- формирует объект заказа;
- отправляет заказ на сервер;
- отображает экран успешного оформления (`Success`).

## Завершение заказа

### `success:close`
Источник: `Success`  
Описание: пользователь завершил сценарий покупки.

Действия презентера:
- закрывает модальное окно;
- очищает корзину;
- очищает данные покупателя;
- сбрасывает состояние форм.

